# Yspilanti Tenant's Union Website

Very early workings of a drupal project. Just wanted the backup/version control for now.

## Dev Setup

In the root directory, simple run the following command:

`sudo ./dev_setup.sh`

